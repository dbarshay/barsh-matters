const fs = require("fs"); const path = require("path"); let failed = false; const pass = (m) => console.log("PASS: " + m); const fail = (m) => { failed = true; console.error("FAIL: " + m); }; const root = process.cwd(); const read = (p) => fs.readFileSync(path.join(root, p), "utf8"); const exists = (p) => fs.existsSync(path.join(root, p)); const pkg = JSON.parse(read("package.json")); const scriptName = "verify:phase31-production-build-confirmation-safety"; if (pkg.scripts && pkg.scripts[scriptName] === "node scripts/verify-phase31-production-build-confirmation-safety.cjs") pass("package script registered"); else fail("package script missing"); if (pkg.scripts && pkg.scripts.build && pkg.scripts.build.includes("prisma generate") && pkg.scripts.build.includes("next build")) pass("build script still runs prisma generate and next build"); else fail("build script contract changed"); if (pkg.dependencies && pkg.dependencies.bcryptjs === "3.0.3") pass("bcryptjs remains pinned exactly"); else fail("bcryptjs exact dependency missing"); if (exists("scripts/verify-admin-auth-prisma-bcrypt-typescript-build-safety.cjs")) pass("admin/auth TypeScript build verifier exists"); else fail("admin/auth TypeScript build verifier missing"); if (exists("scripts/verify-clio-storage-refactor-phase27-finalization-target-preview-route-safety.cjs")) pass("Clio phase 27 verifier exists"); else fail("Clio phase 27 verifier missing"); if (exists("scripts/verify-clio-storage-refactor-phase28-storage-plan-typescript-safety.cjs")) pass("Clio phase 28 verifier exists"); else fail("Clio phase 28 verifier missing"); const route = read("app/api/documents/clio-finalization-target-preview/route.ts"); if (route.includes("previewOnly: true") && route.includes("clioWrite: false") && route.includes("databaseMutation: false") && route.includes("finalizeRewired: false")) pass("Phase 27 preview route remains no-write"); else fail("Phase 27 preview route no-write contract missing"); console.log("RESULT: Phase 31 production build confirmation safety verifier"); if (failed) process.exit(1);